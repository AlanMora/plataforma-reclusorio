# Los scripts `docker:*` solo levantan contenedores: las dependencias se instalan
# DENTRO de la imagen, no en el host. Con esto pnpm NO dispara un `pnpm install`
# en tu máquina antes de correr un script (evita el fallo del "deps status check").
verify-deps-before-run=false
