/** Header used to carry the correlation id across services. */
export const CORRELATION_ID_HEADER = 'x-correlation-id';

/** Key under which the correlation id is stored on the request object. */
export const CORRELATION_ID_KEY = 'correlationId';

/** Header used to forward the authenticated user id from the gateway. */
export const USER_ID_HEADER = 'x-user-id';
